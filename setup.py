from setuptools import setup, find_packages
setup(
      name="texfilter",
      version="0.10",
      description="sensitive word filter",
      author="CBT",
      license="MIT",
      packages= find_packages()
      )
